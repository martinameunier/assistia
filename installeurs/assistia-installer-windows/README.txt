Assistia Windows installation

To install Assistia, simply double-click installer.ps1.

The script installs the required Windows certificate, then automatically starts
assitia-installer.exe from this same folder.

If Windows asks for administrator permission, accept it to continue the
installation.
